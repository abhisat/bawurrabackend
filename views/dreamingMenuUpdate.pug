extends admin

block append content

  form(method='POST' action='/dreamingMenu/editUpdate')
        div.form-group
          label(for='title') Title:
          input#title.form-control(type='text', value=(dreams[0].title), name='title')
        div.form-group
          label(for='media1') Media 1:
          input#media1.form-control(type='file', value=(dreams[0].media_1), name='media1')
        div.form-group
          label(for='body') Body:
          textarea#body.form-control(type='text', name='body') #{dreams[0].body}
        div.form-group
          label(for='media2') Media 2:
          input#media2.form-control(type='file', value=(dreams[0].media_2), name='media2')
        div.form-group
          label(for='video') Video Link(optional):
          input#video.form-control(type='text', value=(dreams[0].video), name='video')

        button.btn.btn-primary(type='submit') Submit
